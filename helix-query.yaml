indices:
  area-vip-es:
    include:
      - /area-vip
      - /area-vip/**
    exclude:
      - /en/**
      - /de/**
      - /fr/**
      - /pt/**
      - /ja/**
      - /hi/**
      - /ar/**
      - /area-vip/fragments/**
    target: /area-vip/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
  area-vip-en:
    include:
      - /en/vip-area
      - /en/vip-area/**
    exclude:
      - /en/vip-area/fragments/**
    target: /en/vip-area/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")
  area-vip-de:
    include:
      - /de/vip-zone
      - /de/vip-zone/**
    exclude:
      - /de/vip-zone/fragments/**
    target: /de/vip-zone/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")
  area-vip-fr:
    include:
      - /fr/zone-vip
      - /fr/zone-vip/**
    exclude:
      - /fr/zone-vip/fragments/**
    target: /fr/zone-vip/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")
  area-vip-pt:
    include:
      - /pt/area-vip
      - /pt/area-vip/**
    exclude:
      - /pt/area-vip/fragments/**
    target: /pt/area-vip/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")
  area-vip-ja:
    include:
      - /ja/vip-area
      - /ja/vip-area/**
    exclude:
      - /ja/vip-area/fragments/**
    target: /ja/vip-area/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")
  area-vip-hi:
    include:
      - /hi/vip-area
      - /hi/vip-area/**
    exclude:
      - /hi/vip-area/fragments/**
    target: /hi/vip-area/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")
  area-vip-ar:
    include:
      - /ar/vip-area
      - /ar/vip-area/**
    exclude:
      - /ar/vip-area/fragments/**
    target: /ar/vip-area/query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: head > meta[property="og:description"]
        value: attribute(el, "content")
      image:
        select: head > meta[property="og:image"]
        value: match(attribute(el, "content"), "https:\/\/[^/]+(/.*)")
      category:
        select: head > meta[name="category"]
        value: attribute(el, "content")
      primary-language-url:
        select: head > meta[name="primary-language-url"]
        value: attribute(el, "content")